{
  "name": "breathe-mail",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm -F web dev",
    "build": "pnpm -F web build",
    "lint": "pnpm -r --filter ./... lint",
    "typecheck": "pnpm -r --filter ./... typecheck",
    "test": "pnpm -r --filter ./... test",
    "format:write": "pnpm -r --filter ./... format:write",
    "scan": "pnpm -F web scan",
    "dead": "pnpm -F web dead",
    "dupes": "pnpm -F web dupes",
    "refactor:check": "pnpm -F web refactor:check"
  },
  "devDependencies": {
    "@ast-grep/cli": "^0.39.5",
    "eslint": "^9.12.0",
    "husky": "^9.1.7",
    "jscpd": "^4.0.5",
    "knip": "^5.65.0",
    "lint-staged": "^16.2.4",
    "msw": "^2.11.5",
    "prettier": "^3.3.3",
    "vitest": "^3.2.4"
  }
}
